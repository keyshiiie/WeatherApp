<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WeatherApp.UI.Resources.Styles.BaseStyles">

    <!-- Базовый стиль для Label -->
    <Style x:Key="BaseLabelStyle" TargetType="Label">
        <Setter Property="FontFamily" Value="OpenSansRegular"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="TextColor" Value="{StaticResource TextPrimaryColor}"/>
        <Setter Property="LineBreakMode" Value="WordWrap"/>
    </Style>

    <!-- Базовый стиль для Button -->
    <Style x:Key="BaseButtonStyle" TargetType="Button">
        <Setter Property="FontFamily" Value="OpenSansRegular"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="TextColor" Value="{StaticResource TextWhiteColor}"/>
        <Setter Property="BackgroundColor" Value="{StaticResource PrimaryColor}"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="HeightRequest" Value="48"/>
        <Setter Property="Padding" Value="20,10"/>
    </Style>

    <!-- Базовый стиль для Entry (переопределяем нативную реализацию) -->
    <Style x:Key="BaseEntryStyle" TargetType="Entry">
        <Setter Property="FontFamily" Value="OpenSansRegular"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="TextColor" Value="{StaticResource TextPrimaryColor}"/>
        <Setter Property="BackgroundColor" Value="{StaticResource InputBackgroundColor}"/>
        <Setter Property="HeightRequest" Value="48"/>
        <Setter Property="PlaceholderColor" Value="{StaticResource InputPlaceholderColor}"/>
        <!-- ВАЖНО: кастомная обводка через VisualStateManager -->
    </Style>
</ResourceDictionary>